% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_Qsplines.R
\name{plot_Qsplines}
\alias{plot_Qsplines}
\title{Plot a Quadratic Spline from Segment Parameters}
\usage{
plot_Qsplines(
  positions,
  speeds,
  accelerations,
  segment_lengths,
  main = "Quadratic Spline",
  col = "blue"
)
}
\arguments{
\item{positions}{Numeric vector of positions at the start of each segment.}

\item{speeds}{Numeric vector of initial speeds for each segment.}

\item{accelerations}{Numeric vector of accelerations for each segment.}

\item{segment_lengths}{Numeric vector of segment lengths.}

\item{main}{Character. Title of the plot.}

\item{col}{Character. Color of the spline curve.}
}
\description{
Plots a piecewise quadratic spline defined by positions, speeds, and accelerations
at the start of each segment.
}
\details{
For each segment \eqn{i}, the spline is defined by:
\deqn{y_i(t) = p_i + v_i t + \tfrac{1}{2} a_i t^2, \quad t \in [0, \text{segment\_length}_i].}
The function ensures positional continuity across segments.
}
\examples{
seg <- c(10, 20, 10)
acc <- c(0.5, -0.2, 0.3)
res <- generate_Qsplines(seg, acc)
plot_Qsplines(res$p[-length(res$p)], res$v, res$a, seg)

}
